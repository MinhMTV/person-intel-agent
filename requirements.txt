# Core
pydantic>=2.6
pydantic-settings>=2.7
httpx>=0.27
typer>=0.12
rich>=13.0
python-dotenv>=1.0

# Web app
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Parsing / network
beautifulsoup4>=4.12
dnspython>=2.4
ddgs>=8.0

# Images & face matching
Pillow>=10.3
numpy>=1.26
# OpenCV 5 no longer ships the Haar cascades DeepFace's "opencv" detector needs.
opencv-python-headless>=4.8,<5
# Primary face embedding model (ArcFace via DeepFace). Without it the app still
# runs, but face identity matching is disabled (detection only).
deepface>=0.0.93; python_version < "3.13"
tf-keras>=2.16; python_version < "3.13"

# Security / exports
cryptography>=42.0
fpdf2>=2.7

# Optional — Google Vision with a service-account JSON (API keys need nothing extra)
# google-auth>=2.29
# Optional — interactive LinkedIn/Xing login (SESSION_MANAGEMENT_ENABLED=true)
# playwright>=1.40

# Development
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.10
